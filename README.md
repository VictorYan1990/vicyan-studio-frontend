# vicyan-studio-frontend

Frontend for Victor Yan's personal website (`https://vicyan.studio`).

## Hosting

Built as static assets and deployed to AWS, served via CloudFront (with the build artifacts stored in S3).

## Run locally

```bash
npm ci
npm start
```

Then open `http://localhost:3000`.

## Build / Test

```bash
npm test
npm run build
```
